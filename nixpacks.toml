# Render deployment config
[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.setuptools", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
